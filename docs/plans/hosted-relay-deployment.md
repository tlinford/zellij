# Hosted Relay Deployment Action Plan

## Goal

Replace the existing development relay on the current VPS with the Stage 2
hosted relay while retaining both browser viewing and native Zellij access.

This is a development deployment. Only test users and test data are allowed.
It is not the final production deployment or final public-domain layout.

## Open-source and self-hosting requirement

All code added on this branch is intended to remain public and open source.
Hosted `zellij.online` operation is one deployment of the relay, not a special
code path that independent operators must depend on.

The implementation must preserve these properties:

- standalone self-hosting remains supported and remains the default relay
  authentication mode;
- hosted mode can target any compatible HTTPS control plane, not only the
  zellij.online website;
- app origin, relay origin, public URL, allowed browser origins, TLS hostname,
  and control-plane URL are operator configuration;
- no secret, account identifier, Cloudflare project name, Workers URL, VPS
  address, or private deployment detail is committed as a required default;
- Cloudflare Pages is a reference deployment target, not a runtime dependency.

The official service is the product being launched and may receive the best
defaults, automation, documentation, and domain conventions. Self-hosting only
needs to remain technically possible through documented configuration; it does
not need to be zero-configuration, support every arbitrary topology, or match
the operational convenience of zellij.online.

Branded values such as `zellij.online` may remain as defaults, examples, and
additional official-service security hardening. They must not make the relay
binary depend on the hosted website or prevent an operator from running
standalone mode with their own app/relay domains. Requiring self-hosters to use
the documented paired-domain convention (`app.example` and
`relay.app.example`) is acceptable for now.

## Current state

- `zellij.online` resolves to the relay VPS and is also used for SSH access.
- `relay.zellij.online` now has an `A` record pointing to the same relay VPS
  (`46.224.157.168`, verified on 2026-08-12).
- The running relay is an older single-origin build. It serves:
  - relay HTTP and WebSocket transport;
  - `/health`;
  - browser-viewer HTML at `/r/<slug>`;
  - viewer assets below `/r/<slug>/assets/`.
- The running stack is the Compose project `zellij-relay`, with relay, nginx,
  and certbot containers.
- Certificates, the certbot webroot, and standalone relay data use persistent
  Docker volumes.
- The observed running relay image was:
  - image ID: `sha256:382810f03ed398397131587c9af6f5ef9f166a3844b625e22db9a365278dfcca`
  - created: `2026-06-17T12:46:04.488780423Z`
- The Stage 2 branch was rebased onto the rewritten `relay/main` on
  2026-08-12:
  - new upstream base: `f6fd24840` (`integration fixes`);
  - hosted-control-plane commit: `f4ecfdd59`;
  - relay-dev CA-path fix: `1358d0431`;
  - hosted deployment commit/current branch tip: `72df56ddc`;
  - pre-rebase backup ref:
    `backup/relay-stage2-hosted-control-plane-pre-rebase-20260812`
    (old tip `4f4c70e6c`).
- Current `relay/main` and the Stage 2 branch use a separated architecture:
  the relay is transport-only and the web viewer must be deployed separately.

Consequently, replacing the live relay with the Stage 2 relay before deploying
an app origin would remove the currently working browser viewer.

### Post-rebase review

The rebased upstream consolidated the versioned browser application into
`v/<version>/assets/app.js` and improved `cargo x relay-dev`, including custom
development hosts. It did not change the production deployment topology:

- production app-origin staging still accepts only `--app-host`;
- the staged CSP still derives `relay.<app-host>`;
- browser runtime code still derives `relay.<location.hostname>`;
- native attachment still derives `relay.<app-host>`;
- relay deployment still accepts `APP_ORIGIN` for public links and CORS.

Therefore, the explicit relay-origin work below remains necessary when the app
origin uses an autogenerated `pages.dev` hostname. Do not refer to the removed
`app-entry.js`; use the generated manifest and consolidated `app.js` artifact.

## Target development topology

```text
Browser
  -> https://<viewer-project>.pages.dev/r/<slug>
  -> https://relay.zellij.online (relay HTTP/WebSocket transport)

Native Zellij sharer/viewer
  -> wss/https://relay.zellij.online

Relay
  -> https://zellij-online-website-dev.linford-t.workers.dev
     (credential verification and lifecycle events)
```

Domain responsibilities:

| Address | Responsibility |
| --- | --- |
| `<viewer-project>.pages.dev` | Temporary static browser app origin |
| `relay.zellij.online` | Relay transport on the existing VPS |
| `zellij-online-website-dev.linford-t.workers.dev` | Development website and control-plane API |
| `zellij.online` | Existing hostname; no longer needed by the relay after migration |

Do not repoint or remove `zellij.online` until `relay.zellij.online` resolves,
TLS works, and SSH access through either `relay.zellij.online` or the VPS IP has
been confirmed.

## Phase 1: Minimal development safety checks

Before changing the running stack:

1. Record the VPS public IPv4 and current DNS records.
2. Record `docker compose ls`, the running containers, volumes, image IDs, and
   public health response for diagnostics. Most of this inventory has already
   been captured; the old relay image is
   `sha256:382810f03ed398397131587c9af6f5ef9f166a3844b625e22db9a365278dfcca`.
3. Record the current TLS SPKI fingerprint.
4. Confirm the three persistent volumes remain present:
   - `zellij-relay_letsencrypt`;
   - `zellij-relay_certbot-webroot`;
   - `zellij-relay_relay-data`.
5. Do not use `deploy.sh destroy`, because it deletes volumes including
   certificates, relay data, and the hosted service-secret volume.
6. Keep all service-secret material outside the repository and Docker build
   context.

This is a development instance with no production workload. Temporary downtime
and loss of the obsolete embedded browser viewer are acceptable. Do not spend
time archiving the old images or reproducing the old Compose deployment; prefer
fixing the new architecture forward. Preserving volumes and secrets is the
safety boundary.

## Phase 2: Establish the relay hostname

1. **Completed:** a DNS-only `A` record now exists:

   ```text
   relay.zellij.online -> 46.224.157.168
   ```

2. Add an `AAAA` record only if the VPS has working public IPv6.
3. **Completed:** `relay.zellij.online` and `zellij.online` both resolve to
   `46.224.157.168` as of 2026-08-12.
4. Verify SSH access through `root@relay.zellij.online` or retain the VPS IP as
   the stable deployment target.
5. Leave `zellij.online` pointing at the VPS until the migration is complete.

The Stage 2 nginx deployment will later obtain a separate Let's Encrypt
certificate for `relay.zellij.online`.

## Phase 3: Make the app origin target an explicit relay

The current app-origin build derives the relay by prepending `relay.` to the
app-origin hostname. That would incorrectly turn a Pages hostname into
`relay.<viewer-project>.pages.dev`.

Add an explicit relay-origin build option, for example:

```sh
cargo x build \
  --app-origin out/ \
  --app-host <viewer-project>.pages.dev \
  --relay-origin https://relay.zellij.online
```

**Implemented locally (pending review/commit):** `--relay-origin` accepts a
validated HTTP/WebSocket origin, normalizes it into paired HTTP and WebSocket
origins, and emits a trusted runtime `<meta>` plus CSP and `_headers` values
from that single configuration. Secure HTTPS/WSS is required remotely;
insecure HTTP/WS remains limited to loopback development. Omitting the option
retains `relay.<app-host>` derivation. The artifact now also generates the
Pages-compatible `_redirects` and `_headers` files. They are intentionally
excluded from `RELEASE_HASHES.txt` because Pages consumes rather than serves
them; deployed routing and response headers must be verified separately.
Generic static hosts may ignore those files and configure equivalent
routing/headers themselves.

The selected relay origin must drive all of the following:

- browser HTTP requests to the relay;
- browser WebSocket connections;
- generated CSP `connect-src` values;
- runtime relay-target validation;
- local-development and app-origin tests.

Requirements:

- Default behavior for the final paired-domain layout may remain
  `<app-host> -> relay.<app-host>`.
- An explicit relay origin must override derivation.
- The option and generated runtime metadata must be generically named; do not
  add a zellij.online-specific or Cloudflare-specific branch.
- Only `https://`/`wss://` remote targets are permitted; loopback development
  may continue to allow HTTP/WebSocket exceptions.
- The relay target must not be accepted from an untrusted share-link query or
  fragment parameter.
- Add tests covering an autogenerated `pages.dev` app origin connected to
  `relay.zellij.online`.
- Generate CSP and browser runtime targeting from the same parsed relay-origin
  configuration so the two cannot drift.

### Future custom-domain simplification (not available for this deployment)

`zellij.online` cannot host the app origin yet. This development deployment
must therefore use the temporary autogenerated `pages.dev` URL and implement
the explicit relay-origin support above.

In a later deployment, assigning `zellij.online` to the app-origin project
would match the upstream convention exactly:

```text
zellij.online        -> app origin
relay.zellij.online  -> relay VPS
```

That future layout would avoid both the explicit relay-origin override and the
native attachment workaround in Phase 5. It is not an option for the current
development deployment and must not block the temporary Pages work.

## Phase 4: Deploy the temporary Cloudflare app origin

1. Build the static app-origin artifact from the same revision as the relay and
   clients.
2. Create a temporary Cloudflare Pages project and note its autogenerated
   `https://<viewer-project>.pages.dev` URL.
3. Publish the generated artifact.
4. Configure the SPA rewrite:

   ```text
   /r/*  /index.html  200
   ```

5. Serve `/assets/*` and `/v/<version>/*` literally.
6. Install the required headers:
   - CSP with `connect-src` restricted to `https://relay.zellij.online` and
     `wss://relay.zellij.online`;
   - HSTS;
   - `X-Content-Type-Options: nosniff`;
   - `X-Frame-Options: DENY`;
   - `Referrer-Policy: no-referrer`.
7. Disable Cloudflare features that rewrite published bytes, including Rocket
   Loader, HTML/JS/CSS Auto Minify, Email Obfuscation, and Mirage.
8. Verify that `/r/test-slug` serves the SPA shell and that every deployed byte
   matches the locally built `out/RELEASE_HASHES.txt`.

   `scripts/verify-app-origin.sh` downloads a manifest from a published GitHub
   release tag, so it cannot verify an unreleased Stage 2 commit as written.
   Before deployment, either extend it with a local-manifest option or add a
   small development verifier that fetches each path listed in the local
   manifest. Once a tagged release exists, also run the published-release
   verification path.

For this development milestone, a manual Pages deployment is acceptable.
GitHub-triggered deployment can be added after the first end-to-end deployment
works.

### Versioned bundle retention

The app-origin build contains a versioned viewer bundle. Future deployments
must preserve previous `/v/<version>/` directories rather than replacing the
entire live tree, so older compatible clients can continue to load their
matching bundles. A one-version development deployment can start manually, but
retention must be solved before treating the deployment workflow as durable.

## Phase 5: Resolve native attachment for the temporary Pages URL

The native attach parser currently transforms:

```text
https://<app-host>/r/<slug>
  -> https://relay.<app-host>/r/<slug>
```

Therefore, a native client given the temporary Pages share URL would derive a
nonexistent `relay.<viewer-project>.pages.dev` hostname.

For the initial development deployment, use two equivalent forms of the link:

- browsers use
  `https://<viewer-project>.pages.dev/r/<slug>#k=<secret>`;
- native clients use
  `https://relay.zellij.online/r/<slug>#k=<secret>`.

To attach natively, manually replace only the Pages hostname with
`relay.zellij.online`. Preserve the slug, `#k` secret, and any other fragment
parameters exactly. This needs no additional URL protocol or client work and is
the selected workaround for the first deployment.

Self-hosters can continue to use either direct relay URLs or the existing
paired-domain convention. Phase 11 records the follow-up client change that
will remove the temporary two-link behavior for unrelated app and relay
origins.

## Phase 6: Verify website control-plane readiness

Against `https://zellij-online-website-dev.linford-t.workers.dev`:

1. Confirm restricted development mode remains enabled.
2. Confirm all D1 migrations are applied.
3. Create a test user and hosted relay credential.
4. Confirm the control plane's `RELAY_VERIFY_SECRETS` includes the service
   secret stored in the protected local deployment file.
5. Exercise the verify and event endpoints with test data.
6. Confirm invalid and revoked credentials fail closed.
7. Ensure the service-secret file is outside the repository and Docker build
   context, is non-empty, and has restrictive local permissions.

## Phase 6a: Portability and hard-coded-domain audit

Before deployment, audit the Stage 2 diff and the affected upstream surfaces
for deployment-specific constants. At minimum review:

- relay defaults and environment parsing in `zellij-relay-server/src/config.rs`;
- deployment defaults in `zellij-relay-server/deploy/`;
- app-origin/CSP generation in `xtask/src/build.rs`;
- browser relay targeting in `zellij-web-client-assets/assets/utils.js`;
- browser E2E enforcement and known-host handling in
  `zellij-web-client-assets/assets/auth.js` and `modals.js`;
- native URL resolution and known-host handling in
  `zellij-relay-client/src/attach/mod.rs`.

Classify every `zellij.online` occurrence as one of:

1. official-service example or replaceable default — acceptable;
2. test fixture for the official service — acceptable if a neutral
   standalone/configuration case is also covered where relevant;
3. a dependency that prevents standalone operation or documented paired-domain
   self-hosting — must be generalized.

The hard-coded `KNOWN_RELAY_HOSTS` lists provide additional downgrade hardening
for the official service. They may remain branded for now as long as ordinary
self-hosted E2E operation still works and the list does not redirect or block a
configured self-hosted relay. Do not allow an untrusted URL parameter to grant
trusted-relay status.

**Audit result for this milestone:** the relay server and deployment values are
replaceable defaults/examples, and hosted control-plane mode remains selected
only by explicit control-plane configuration. The browser/native known-host
lists only force E2E hardening for the official service; configured or neutral
self-hosted relays are not redirected or rejected. Browser and native URL
fallbacks retain the documented paired-domain rule, while direct native relay
URLs remain supported. No additional branded occurrence blocks standalone or
paired-domain self-hosting, so no further domain generalization is included in
this milestone.

Minimal acceptance checks for self-hosting:

- build and stage a viewer using a neutral paired-domain example;
- verify its CSP and browser runtime target the derived/configured relay;
- verify relay CORS and WebSocket `Origin` checks accept the configured app
  origin;
- verify native attachment works with the paired-domain convention;
- verify standalone mode starts with no control-plane configuration;
- verify hosted mode works against a generic mock control plane.

## Phase 7: Prepare matching artifacts

Start implementation from the rebased Stage 2 tip `72df56ddc`. After the
app-origin work is committed, use a clean checkout pinned to that final reviewed
deployment revision. Do not include unrelated local changes, plan files, or
development certificates in the build context.

Build from the same revision:

- relay server image;
- app-origin artifact;
- native Zellij sharer binary;
- native Zellij viewer binary.

Stage 2 revised the pre-release relay protocol in place, so old deployed-era
clients are not expected to interoperate with the Stage 2 relay.

## Phase 8: Deploy the hosted relay

Deploy to the existing VPS with these distinct settings:

```text
SSH/deployment target:       zellij.online or the VPS IP
Relay public host:           relay.zellij.online
App origin:                  https://<viewer-project>.pages.dev
Control-plane URL:           https://zellij-online-website-dev.linford-t.workers.dev
```

Example shape:

```sh
APP_ORIGIN=https://<viewer-project>.pages.dev \
./deploy.sh deploy \
  --mode hosted \
  --vps-ip zellij.online \
  --vps-user root \
  --public-host relay.zellij.online \
  --control-plane-url https://zellij-online-website-dev.linford-t.workers.dev \
  --control-plane-secret-file /secure/path/relay-service-secret \
  --le-email <email>
```

Expected effects:

- nginx obtains and serves a certificate for `relay.zellij.online`;
- the relay generates Pages viewer URLs;
- CORS allows only the configured Pages app origin;
- the relay validates tunnel credentials through the development website;
- lifecycle events are sent to the development website;
- the control-plane secret is mounted from the dedicated Docker volume and is
  not stored in the image or container environment.

Expect a short interruption to active tunnels when containers are recreated.

## Phase 9: Post-deployment verification

### Infrastructure

- `https://relay.zellij.online/health` returns `200` and `ok`.
- TLS is valid for `relay.zellij.online`.
- The relay, nginx, and certbot containers are healthy.
- The relay logs show hosted mode without exposing credentials.
- The control-plane secret is absent from container environment, Compose
  rendering, image history, command arguments, and logs.
- The mounted secret is read-only and has the expected ownership and mode.
- The certificate and data volumes remain present.

### Browser viewer

1. A test user creates a hosted relay credential.
2. A matching Stage 2 Zellij build opens a tunnel.
3. The generated URL points to the Pages app origin.
4. The viewer loads its matching versioned bundle.
5. Browser HTTP and WebSocket traffic targets `relay.zellij.online`.
6. A correct credential permits attachment.
7. Incorrect and revoked credentials are rejected without revealing whether a
   slug exists.
8. Read-only, read/write, resize, reconnect, and clean disconnect work.
9. Browser developer tools show no CSP or CORS violations.

### Native client

- Attach using the selected temporary native URL strategy.
- Verify read-only and read/write behavior, resize, reconnect, and disconnect.
- Confirm TLS pinning records the relay hostname and that the published SPKI
  matches the live certificate.

### Control plane

- Tunnel start appears in the development dashboard.
- Tunnel stop is recorded after disconnect.
- Invalid or revoked credentials cannot open a new tunnel.
- Control-plane unavailability causes new tunnel authorization to fail closed.
- Existing authorized tunnels follow the documented authorize-once semantics.

## Phase 10: Stabilize or use a temporary fallback

The goal is to stabilize the new separated architecture. Temporary relay
downtime during development is acceptable, and failure of one acceptance test
does not require restoring the obsolete embedded-viewer deployment.

If hosted integration fails but the new relay is otherwise sound, redeploy the
new stack in standalone mode using the preserved relay-data volume while the
control-plane issue is fixed.

If the new protocol, transport, or viewer is broken, diagnose and fix forward.
The recorded old image ID is only a last-resort diagnostic fallback; restoring
the current embedded browser viewer is not a deployment requirement. Never use
`destroy` as a recovery step.

After a successful deployment, record:

- deployed source commit;
- relay and nginx image IDs;
- app-origin build revision and Pages deployment identifier;
- website deployment revision;
- DNS records and TLS SPKI fingerprint;
- test results and known limitations;
- the chosen temporary native-link behavior.

## Phase 11: Add explicit relay origins to native share-link handling

After the initial hosted deployment works with the Phase 5 workaround, extend
the share-link format and native client so one generated URL works for both the
Pages viewer and native Zellij attachment. A candidate format is:

```text
https://<viewer-project>.pages.dev/r/<slug>#k=<secret>&relay=https%3A%2F%2Frelay.zellij.online
```

Implementation requirements:

- link generation includes the configured public relay origin;
- the native client reads and validates the explicit relay origin before
  falling back to the existing `relay.<app-host>` derivation;
- remote relay origins require HTTPS/WSS, while HTTP/WS remains restricted to
  loopback development;
- credentials, paths, queries, fragments, malformed hosts, and ambiguous ports
  in the relay-origin value are rejected;
- the browser continues to use only its trusted build-generated relay target
  and must not allow the share-link parameter to override CSP or runtime relay
  selection;
- the key and relay metadata remain in the URL fragment so the app-origin host
  does not receive them in HTTP requests;
- direct relay URLs and existing paired-domain links remain compatible;
- tests cover explicit unrelated domains, legacy fallback behavior, malformed
  inputs, downgrade rejection, and preservation of `#k` plus other fragment
  parameters.

The relay origin is routing metadata rather than a secret. The attachment key
remains the sensitive value. This change should stay generic and configurable;
do not hardcode the Pages project or `zellij.online` into native resolution.

## Deferred work

- Move the app origin and website to their final public hostnames.
- Decide the final ownership of `zellij.online`.
- Automate app-origin deployment from GitHub.
- Preserve versioned app bundles automatically across deployments.
- Define the final policy for backward compatibility with older relay clients.
- Add production monitoring, alerting, backups, rate limits, and incident
  runbooks before calling the service production-ready.

## Completion criteria

The development deployment is complete when:

- `relay.zellij.online` serves the Stage 2 hosted relay with valid TLS;
- the temporary Pages origin serves the verified browser bundle;
- browser viewing works through the separated origins;
- matching native Stage 2 clients can share and attach;
- hosted credentials and lifecycle events work through the development
  website;
- secrets do not appear in images, environment, arguments, or logs;
- certificate, relay-data, and hosted-secret volumes remain intact;
- the minimal paired-domain self-hosting checks pass without modifying source
  code.
