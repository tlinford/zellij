name: zellij-relay

services:
  relay:
    build:
      context: ../..
      dockerfile: zellij-relay-server/deploy/Dockerfile
    environment:
      RELAY_BIND_ADDR: "0.0.0.0:8765"
      RELAY_PUBLIC_URL_TEMPLATE: "${APP_ORIGIN:-https://zellij.online}/r/{slug}"
      RELAY_ALLOWED_ORIGINS: "${APP_ORIGIN:-https://zellij.online}"
      RELAY_DATA_DIR: "/var/lib/zellij-relay"
      RUST_LOG: "info"
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    expose:
      - "8765"
    volumes:
      - relay-data:/var/lib/zellij-relay

  nginx:
    build:
      context: ./nginx
      args:
        PUBLIC_HOST: "${PUBLIC_HOST:-relay.zellij.online}"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - letsencrypt:/etc/letsencrypt:ro
      - certbot-webroot:/var/www/certbot:ro
    depends_on:
      - relay
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
      - CHOWN
      - SETUID
      - SETGID

  # Long-running certbot for unattended renewal. The initial cert is obtained
  # by bootstrap-cert.sh before this stack comes up; from then on renew runs
  # every 12h via the webroot that nginx serves.
  certbot:
    image: certbot/certbot:latest
    volumes:
      - letsencrypt:/etc/letsencrypt
      - certbot-webroot:/var/www/certbot
    entrypoint:
      - sh
      - -c
      - |
        trap exit TERM
        while :; do
          certbot renew --reuse-key --quiet --webroot -w /var/www/certbot
          sleep 12h & wait $${!}
        done
    restart: unless-stopped
    depends_on:
      - nginx

volumes:
  letsencrypt:
  certbot-webroot:
  relay-data:
